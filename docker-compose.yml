# docker-compose.yml — Service web Nginx
version: '3.9'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8080:80'
    environment:
      - BACKEND_URL=${BACKEND_URL:-http://localhost:8000}
    restart: unless-stopped
