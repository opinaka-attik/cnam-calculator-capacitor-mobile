# APK générés
artifacts/

# Projet Android généré par Capacitor (à régénérer via Docker)
android/

# Dépendances Node
node_modules/

# public/config.js est COMMITÉ (config statique pour l'APK Android)
# Ne pas l'ignorer ici — seul le service Nginx web le génère dynamiquement

# Logs
*.log
